name: esc_pos_printer
description: The library allows to print receipts using an ESC/POS thermal WiFi printer.
version: 4.1.1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  charset_converter: ^2.0.0
  image: ^4.5.4
  esc_pos_utils:
    git:
      url: https://github.com/mu7mmd/esc_pos_utils.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
